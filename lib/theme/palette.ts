import type { OrgPalette } from "./derive";

/**
 * The palette Sous wears before an org has picked its colours: deep sea green,
 * warm copper, cream paper. Calm, precise, warm — the ledger default.
 */
export const DEFAULT_PALETTE: OrgPalette = {
  primary: "#2E6158",
  accent: "#B56E3C",
  tint: "#F7F3EA",
};
